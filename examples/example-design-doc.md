# 0904-이미지-업로드-파이프라인

## 메타데이터
- **생성일**: 2026-09-04
- **상태**: 설계

---

## 현재 설계
<!-- CURRENT:START -->
### D-2 · validation · 검증은 업로드 완료 콜백에서
presigned URL은 서버를 거치지 않으므로 업로드 시점에 검증할 수 없다. 완료 콜백에서 크기·MIME·매직바이트를 확인하고, 실패하면 키를 격리 접두사로 옮긴다.

지우지 않는 이유는 오탐 때 되돌릴 수 없어서다.

### D-3 · storage · 원본도 CDN 뒤에 둔다
원본 직접 접근이 필요한 경로가 생겼다(재처리 배치). 버킷을 공개하지 않고 CDN 서명 URL로 연다.

D-1의 '원본은 비공개' 전제를 바꾼다. 스토리지 배치 자체는 그대로다.
<!-- CURRENT:END -->

---

## 작업 큐

### S-1 · presigned URL 발급 + 키 스키마
- 담당: D-3
- 대상: `api/**/upload*.py`, `core/storage/**`
- acceptance: 발급 API가 키와 만료를 반환한다. 키에 사용자 스코프가 들어간다
- status: 승인

### S-2 · 업로드 완료 콜백 검증
- 담당: D-2
- 대상: `api/**/callback*.py`, `core/media/validate*.py`
- acceptance: 매직바이트 불일치 시 격리 접두사로 이동. 원본은 삭제하지 않는다
- status: 검수대기

### S-3 · 썸네일 온디맨드 + CDN 캐시 헤더
- 담당: D-3
- 대상: `api/**/thumb*.py`, `infra/cdn/**`
- acceptance: 최초 요청에 생성되고 두 번째 요청은 CDN에서 나간다
- status: 대기

---

## 결정 로그
<!-- LEDGER:START -->
### D-1 · 원본은 객체 스토리지, 썸네일은 CDN 오리진
- **status**: SUPERSEDED by D-3
- **topic**: storage
- **decided**: 2026-09-04

원본을 DB에 넣지 않는다. 업로드는 presigned URL로 클라이언트가 직접 올리고, 서버는 키만 받는다.

썸네일은 요청 시 생성하고 CDN이 캐시한다. 미리 만들어두면 안 쓰는 크기까지 만들게 된다.

### D-2 · 검증은 업로드 완료 콜백에서
- **status**: ACTIVE
- **topic**: validation
- **decided**: 2026-09-04

presigned URL은 서버를 거치지 않으므로 업로드 시점에 검증할 수 없다. 완료 콜백에서 크기·MIME·매직바이트를 확인하고, 실패하면 키를 격리 접두사로 옮긴다.

지우지 않는 이유는 오탐 때 되돌릴 수 없어서다.

### D-3 · 원본도 CDN 뒤에 둔다
- **status**: ACTIVE
- **topic**: storage
- **decided**: 2026-09-04

원본 직접 접근이 필요한 경로가 생겼다(재처리 배치). 버킷을 공개하지 않고 CDN 서명 URL로 연다.

D-1의 '원본은 비공개' 전제를 바꾼다. 스토리지 배치 자체는 그대로다.
<!-- LEDGER:END -->

---

## 진행 로그
- 2026-09-04: D-1 확정 — 원본은 객체 스토리지, 썸네일은 CDN 오리진
- 2026-09-04: D-2 확정 — 검증은 업로드 완료 콜백에서
- 2026-09-04: D-3 확정 — 원본도 CDN 뒤에 둔다 (supersedes D-1)
- 2026-09-04: [검수] S-1 게이트 GREEN, 승인
- 2026-09-04: [검수] S-2 설계 밖 변경 1건 — 재처리 배치용 원본 접근. D-3로 확정
